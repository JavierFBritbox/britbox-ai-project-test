# apps/

The solution code. One subdirectory per **independently deployable module**. Each module owns
its own technology stack and build/test/lint tooling, and may carry a module-scoped `CLAUDE.md`
with instructions specific to it.

_(empty — modules are created by the pipeline once features are defined)_
